# Forecasting API

Monthly forecast and scenario endpoints.

- `GET /forecasts/monthly/{project_id}` - Monthly cost forecast
- `GET /forecasts/scenarios/{project_id}` - Best/worst/most_likely scenarios
