# Backend Environment Template
# Copy to .env and fill in values. See docs/security/env-setup.md for details.

# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# JWT verification (required for Audit Dashboard and protected APIs)
# Supabase Dashboard → Project Settings → API → JWT Secret (not Anon/Service Role key)
SUPABASE_JWT_SECRET=your-jwt-secret-here

# Optional: allow default admin when no/invalid token (development only)
# Set to true only for local dev; never in production.
# ALLOW_DEV_DEFAULT_USER=true

# Base URL (e.g. for links in emails)
BASE_URL=http://localhost:8000
# ENVIRONMENT=development
